# Stripe publishable key (test or live) for Firebase extension checkout redirect
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Optional: set in .env.local if dev compile fails on source-map-loader ENOENT
GENERATE_SOURCEMAP=false
